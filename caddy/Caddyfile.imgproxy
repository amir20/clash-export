0.0.0.0 {
    cache {
        default_max_age 1h
    }

    proxy / imgproxy:8080 {
        transparent
    }

    log / stdout "[{when}] \"{method} {uri} {proto}\" {status} {size} \"{>User-Agent}\""
    errors stdout
}
