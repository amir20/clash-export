on: push
name: Test
jobs:
  docker-build:
    name: Build & Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Build Docker images
        run: make build
      - name: Run tests
        run: make test
      - name: Run integeration tests
        run: make int
